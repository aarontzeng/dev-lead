name: lint

on:
  push:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repo invariants
        run: python3 scripts/lint.py
      - name: Shell helper tests
        run: python3 scripts/test_scripts.py
      - name: Shell syntax
        run: for f in scripts/*.sh; do bash -n "$f"; done
